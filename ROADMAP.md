# Squire Roadmap

> 已决定延后的未来项，record-only；本文只记录，不自行排序、排期或判断价值。

## 项目健康

- **文档结构质量审计** — 为 doctor 的随装 checker 增加 advisory finding，识别 durable docs 中的括号堆叠、inline qualification 和累积式补丁等候选信号，并排除 `plans/`；doctor 再判断应保留、拆分、合并重写还是删除，避免局部补充演变成结构性 patching。
